export interface Banner {
  year: string;
  duration: string;
  id: string;
  plot: string;
  title: string;
  genre: string;
  poster: string;
}

export interface Tab {
  title: string;
  emoji: string;
  id: string;
}
